# npm registry configuration
registry=https://registry.npmjs.org/

# Package scope configuration
@eecho:registry=https://registry.npmjs.org/

# Publishing configuration
publish-config-registry=https://registry.npmjs.org/

# Security settings
audit-level=moderate

# Package access (for scoped packages)
access=public

# Prevent accidental publishing to wrong registry
# Always use npm official registry for @eecho packages

# Optional: Set author information (uncomment if needed)
# init-author-name=eecho
# init-author-email=your-email@example.com
# init-license=ISC

# Workspace configuration
workspaces-update=false